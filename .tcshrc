umask 022
setenv LC_ALL en_US.UTF-8

set nobeep
set autolist
set notify
set color
#tcsh use setenv as export in bash, remamber to escape :
#setenv PATH $PATH\:/usr/local/go/bin
#setenv GOPATH "$HOME/go/"

#make ls looks better in colors
setenv CLICOLOR yes
setenv LSCOLORS GxFxFxDxBxegedabagGxGx
#1. directory
#2. symbolic link
#3. socket
#4. pipe
#5. executable
#6. block special
#7. character special
#8. executable with setuid bit set
#9. executable with setgid bit set
#10.directory writable to others, with sticky bit
#11.directory writable to others, without sticky bit

#alias <ins> <alias ins>
#use -F to show file attribute
alias ls 'ls -hlF'
alias ll 'ls -hlaF'
alias h  'history'
#alias sd  'cd /Volumes/SD/'
#for macbook:replace built-in vim
alias vim '/usr/local/bin/vim'
alias vimdiff '/usr/local/bin/vimdiff'
alias git '/usr/local/bin/git'
alias kevin 'ssh oneof@140.113.27.57 -p 7122'

if ($?prompt) then
    set prompt="%{^[[1;36m%}%T%{^[[m%}%{^[[1;33m%}%n%{^[[m%}@%{^[[1;37m%}%m%{^[[1;32m%}[%~]%{^[[m%} >"
endif
